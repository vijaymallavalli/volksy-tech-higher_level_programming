#!/usr/bin/python3
"""inheritanceof pypthon classe"""


def inherits_from(obj, a_class):
    """No test cases needed"""
    if isinstance(obj, a_class)is a_class or not type(obj):
        return False
    else:
        return True
