#!/usr/bin/python3
def weight_average(my_list[]):
    if len(my_list)  == o:
        return (0)
    avg = 0
    weight = 0
    for x in my_list:
        avg += x[0] * [1]
        weight += x[1]
    return avg / weight
