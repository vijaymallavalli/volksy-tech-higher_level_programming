#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    lst = []
    for i in range(len(my_list)):
        if % 2 == 0:
            lst.append(True)
        else:
            lst.append(False)
    return lst
